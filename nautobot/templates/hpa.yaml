{{- if .Values.nautobot.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "nautobot.fullname" . }}
  labels:
    {{- include "nautobot.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "nautobot.fullname" . }}
  minReplicas: {{ .Values.nautobot.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.nautobot.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.nautobot.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.nautobot.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.nautobot.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.nautobot.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
